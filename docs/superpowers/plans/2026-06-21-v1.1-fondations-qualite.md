# Plan d'implémentation — v1.1 « Fondations qualité »

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Durcir le projet avant publication — nettoyer la référence au batch absent, déclarer Python 3.10+, unifier le versioning (1.1.0 + CHANGELOG), ajouter une CI GitHub Actions et des tests de robustesse du parsing.

**Architecture:** Quatre volets indépendants appliqués sur la branche `feat/v1.1-fondations-qualite`. Aucune nouvelle fonctionnalité utilisateur hormis `--version` côté viewer. Les modifications de code touchent `cdp_scraper.py` (retraits) et `cdp_viewer.py` (version), les tests s'ajoutent dans `tests/` avec un dossier `tests/fixtures/` pour les pages HTML longues, et l'infra ajoute `.github/workflows/ci.yml` et `CHANGELOG.md`.

**Tech Stack:** Python 3.10+, bibliothèque standard (`unittest`, `argparse`, `subprocess`), `requests` (scraper uniquement), GitHub Actions.

**Référence spec:** [docs/superpowers/specs/2026-06-21-v1.1-fondations-qualite-design.md](../specs/2026-06-21-v1.1-fondations-qualite-design.md)

**Branche:** `feat/v1.1-fondations-qualite` (déjà créée et active).

---

## Task 1 : S6 — Retrait du code batch dans `cdp_scraper.py`

**Files:**
- Modify: `cdp_scraper.py`

- [ ] **Step 1 : Retirer le commentaire périmé au-dessus de `DEPOT`**

Remplacer (lignes 37-39) :

```python
# URL du dépôt, reprise dans le User-Agent (transparence vis-à-vis du serveur).
# Remplacez par l'URL réelle après publication.
DEPOT = "https://github.com/Bastien-Gaffet/cdp-scraper"
```

par :

```python
# URL du dépôt, reprise dans le User-Agent (transparence vis-à-vis du serveur).
DEPOT = "https://github.com/Bastien-Gaffet/cdp-scraper"
```

- [ ] **Step 2 : Supprimer la fonction `sauvegarder_cookies`**

Supprimer entièrement ce bloc (lignes 188-196, fonction + ligne vide qui suit) :

```python
def sauvegarder_cookies(session: requests.Session, chemin: Path):
    """Écrit les cookies (CDP_SESSION…) au format attendu par telechargeur_batch.py."""
    with open(chemin, "w", encoding="utf-8") as f:
        f.write("# Cookies de session cahier-de-prepa\n")
        f.write(f"# Utilisable avec : python telechargeur_batch.py urls.txt --cookie-fichier {chemin}\n\n")
        for c in session.cookies:
            f.write(f"{c.name}={c.value}\n")
    print(vert(f"Cookies sauvegardés : {chemin}"))

```

Le commentaire de section `# ─── Analyse des pages « docs » ───` qui suivait reste en place.

- [ ] **Step 3 : Retirer l'exemple `--liste`/`--cookie-sortie` de l'epilog**

Dans `parse_args`, supprimer cette ligne de l'epilog (ligne 582) :

```python
  python cdp_scraper.py --url https://... --liste urls.txt --cookie-sortie cookies.txt
```

L'epilog conserve les autres exemples (interactif, `--url`, `--simulation`).

- [ ] **Step 4 : Supprimer l'argument `--liste`**

Supprimer ces deux lignes (593-594) :

```python
    p.add_argument("--liste", metavar="FICHIER",
                   help="Exporter les URLs au lieu de télécharger (pour telechargeur_batch.py)")
```

- [ ] **Step 5 : Supprimer l'argument `--cookie-sortie`**

Supprimer ces deux lignes (603-604) :

```python
    p.add_argument("--cookie-sortie", metavar="FICHIER",
                   help="Sauvegarder les cookies de session dans un fichier")
```

- [ ] **Step 6 : Supprimer le bloc d'appel à `sauvegarder_cookies` dans `main`**

Supprimer ce bloc (lignes 645-647, avec sa ligne vide finale) :

```python
    if args.cookie_sortie:
        sauvegarder_cookies(session, Path(args.cookie_sortie))

```

- [ ] **Step 7 : Supprimer le bloc d'export `--liste` dans `main`**

Supprimer ce bloc complet (lignes 672-686, le commentaire d'en-tête, le `if args.liste:` et la ligne vide qui suit) :

```python
    # ── Export liste seule ───────────────────────────────────────────────────
    if args.liste:
        with open(args.liste, "w", encoding="utf-8") as f:
            f.write(f"# URLs de documents — {url}\n")
            f.write(f"# python telechargeur_batch.py {args.liste} --cookie-fichier cookies.txt\n\n")
            for d in sorted(documents, key=lambda x: (x.get("chemin", ""), x["nom"])):
                if "url" not in d:          # programme de colles textuel : pas d'URL
                    continue
                chemin = d.get("chemin", "")
                f.write(f"{d['url']}  # {chemin + '/' if chemin else ''}{d['nom']}\n")
        print(vert(f"Liste exportée : {args.liste}  ({len(documents)} URLs)"))
        if not args.cookie_sortie:
            print(jaune("Astuce : ajoutez --cookie-sortie cookies.txt pour pouvoir télécharger ensuite."))
        sys.exit(0)

```

Le bloc suivant (`# ── Téléchargement ──`) devient l'étape qui suit immédiatement le résumé du nombre de documents.

- [ ] **Step 8 : Vérifier qu'aucune référence batch ne subsiste dans le code**

Run :

```bash
grep -nE "telechargeur_batch|cookie-sortie|cookie_sortie|args\.liste|--liste|sauvegarder_cookies" cdp_scraper.py
```

Expected : aucune sortie.

- [ ] **Step 9 : Vérifier que l'aide ne mentionne plus rien**

Run :

```bash
python cdp_scraper.py --help
```

Expected : la sortie ne contient ni `--liste`, ni `--cookie-sortie`, ni `telechargeur_batch`. Le `--help` s'affiche sans erreur (l'import déclenche `_assurer_dependances` ; `requests` doit être installé).

- [ ] **Step 10 : Lancer la suite de tests existante**

Run :

```bash
python -m unittest discover -s tests
```

Expected : OK (tous les tests existants passent — aucun ne référençait le code retiré).

- [ ] **Step 11 : Commit**

```bash
git add cdp_scraper.py
git commit -m "$(cat <<'EOF'
refactor(scraper): retire telechargeur_batch (--liste, --cookie-sortie, sauvegarder_cookies)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2 : S6 — Nettoyage de la documentation

**Files:**
- Modify: `docs/cdp_scraper_doc.md`

- [ ] **Step 1 : Retirer l'exemple batch du mode arguments**

Remplacer (lignes 40-41) :

```bash
# Exporter la liste des URLs (pour telechargeur_batch.py) + cookies
python cdp_scraper.py --url https://cahier-de-prepa.fr/ma-classe --liste urls.txt --cookie-sortie cookies.txt
```

par (supprimer ces deux lignes ; le bloc d'exemples se termine alors sur l'exemple `--simulation`).

- [ ] **Step 2 : Retirer les lignes `--liste` et `--cookie-sortie` du tableau des arguments**

Supprimer ces deux lignes du tableau (lignes 56 et 61) :

```markdown
| `--liste FICHIER` | string | — | Exporter les URLs trouvées (compatible `telechargeur_batch.py`) |
```

```markdown
| `--cookie-sortie FICHIER` | string | — | Sauvegarder les cookies de session |
```

- [ ] **Step 3 : Supprimer toute la section « Compatibilité avec telechargeur_batch.py »**

Supprimer le bloc complet (lignes 131-142) :

```markdown
## 🔗 Compatibilité avec telechargeur_batch.py

Le flag `--liste` génère un fichier d'URLs (avec le chemin de chaque document en commentaire). Couplé à `--cookie-sortie`, il permet de séparer découverte et téléchargement :

```bash
# Étape 1 : découvrir, exporter les URLs + cookies
python cdp_scraper.py --url https://cahier-de-prepa.fr/ma-classe --liste urls.txt --cookie-sortie cookies.txt

# Étape 2 : télécharger en parallèle (4 threads)
python telechargeur_batch.py urls.txt --cookie-fichier cookies.txt -t 4 -s ./cours
```
```

- [ ] **Step 4 : Vérifier qu'aucune référence batch ne subsiste dans la doc**

Run :

```bash
grep -rnE "telechargeur_batch|cookie-sortie|--liste" docs/ README.md
```

Expected : aucune sortie (les seules occurrences restantes acceptables sont dans les specs/plans `docs/superpowers/` et le futur `CHANGELOG.md`, exclus par le périmètre — ici on vise `docs/cdp_scraper_doc.md` et `README.md`).

> Note : si la commande renvoie des lignes provenant de `docs/superpowers/specs/` ou `docs/superpowers/plans/`, c'est normal (ce sont les documents de conception). Vérifier seulement qu'il n'en reste pas dans `docs/cdp_scraper_doc.md` ni `README.md`.

- [ ] **Step 5 : Commit**

```bash
git add docs/cdp_scraper_doc.md
git commit -m "$(cat <<'EOF'
docs: retire les references a telechargeur_batch de la documentation

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3 : Python 3.10+ dans la documentation

**Files:**
- Modify: `README.md:52`
- Modify: `docs/cdp_scraper_doc.md` (si une version y est mentionnée)

- [ ] **Step 1 : Mettre à jour la version requise dans le README**

Dans `README.md` (ligne 52), remplacer :

```markdown
Python ≥ 3.8 requis (seule dépendance : `requests`).
```

par :

```markdown
Python ≥ 3.10 requis (seule dépendance : `requests`).
```

- [ ] **Step 2 : Vérifier l'absence de mention résiduelle de 3.8/3.9 comme minimum**

Run :

```bash
grep -rnE "3\.8|3\.9" README.md docs/cdp_scraper_doc.md
```

Expected : aucune sortie présentant 3.8 ou 3.9 comme version minimale requise. Si une occurrence apparaît dans `docs/cdp_scraper_doc.md`, l'aligner sur « 3.10 » de la même façon.

- [ ] **Step 3 : Commit**

```bash
git add README.md docs/cdp_scraper_doc.md
git commit -m "$(cat <<'EOF'
docs: declare Python 3.10+ comme version minimale requise

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4 : P4 — Versioning unifié (scraper + viewer 1.1.0, `--version` viewer)

**Files:**
- Modify: `cdp_scraper.py:36`
- Modify: `cdp_viewer.py` (ajout `__version__` + argument `--version`)
- Test: `tests/test_cdp_scraper.py` (garde-fou d'égalité), `tests/test_cdp_viewer.py` (CLI `--version`)

- [ ] **Step 1 : Écrire le test garde-fou de version (échoue d'abord)**

Dans `tests/test_cdp_scraper.py`, ajouter l'import du viewer en haut, juste après `import cdp_scraper` (ligne 10) :

```python
import cdp_viewer
```

Puis ajouter cette classe de test avant le bloc `if __name__ == "__main__":` :

```python
class TestVersion(unittest.TestCase):
    """Le versioning est unifié : scraper et viewer partagent la même version."""

    def test_versions_egales(self):
        self.assertEqual(cdp_scraper.__version__, cdp_viewer.__version__)

    def test_version_attendue(self):
        self.assertEqual(cdp_scraper.__version__, "1.1.0")
```

- [ ] **Step 2 : Lancer le test pour le voir échouer**

Run :

```bash
python -m unittest tests.test_cdp_scraper.TestVersion -v
```

Expected : FAIL — `cdp_viewer` n'a pas d'attribut `__version__` (AttributeError) et `cdp_scraper.__version__` vaut encore `"1.0.0"`.

- [ ] **Step 3 : Bumper la version du scraper**

Dans `cdp_scraper.py` (ligne 36), remplacer :

```python
__version__ = "1.0.0"
```

par :

```python
__version__ = "1.1.0"
```

- [ ] **Step 4 : Ajouter `__version__` au viewer**

Dans `cdp_viewer.py`, après le bloc d'imports et avant `PAGE_HTML = r"""...` (entre la ligne 23 `from pathlib import Path` et la ligne 25), insérer :

```python

__version__ = "1.1.0"
```

- [ ] **Step 5 : Ajouter l'option `--version` au parseur du viewer**

Dans `cdp_viewer.py`, fonction `main`, après l'argument `--no-browser` et avant `args = p.parse_args()` (ligne 906), insérer :

```python
    p.add_argument("--version", action="version",
                   version=f"cdp-viewer {__version__}")
```

- [ ] **Step 6 : Relancer le test garde-fou**

Run :

```bash
python -m unittest tests.test_cdp_scraper.TestVersion -v
```

Expected : PASS (2 tests).

- [ ] **Step 7 : Écrire le test CLI `--version` du viewer**

Dans `tests/test_cdp_viewer.py`, ajouter cette classe de test (placer près des autres classes de test, avant `if __name__ == "__main__":`). Vérifier en tête de fichier que `import sys`, `import subprocess` et `from pathlib import Path` sont présents ; sinon les ajouter aux imports existants.

```python
class TestVersionCLI(unittest.TestCase):
    """`cdp_viewer.py --version` affiche la version et quitte proprement."""

    def test_version_cli(self):
        racine_projet = Path(__file__).resolve().parent.parent
        script = racine_projet / "cdp_viewer.py"
        res = subprocess.run(
            [sys.executable, str(script), "--version"],
            capture_output=True, text=True, timeout=30,
        )
        self.assertEqual(res.returncode, 0)
        self.assertIn("cdp-viewer 1.1.0", res.stdout)
```

- [ ] **Step 8 : Lancer le test CLI**

Run :

```bash
python -m unittest tests.test_cdp_viewer.TestVersionCLI -v
```

Expected : PASS.

- [ ] **Step 9 : Vérifier manuellement les deux CLI**

Run :

```bash
python cdp_scraper.py --version
python cdp_viewer.py --version
```

Expected : `cdp-scraper 1.1.0` puis `cdp-viewer 1.1.0`.

- [ ] **Step 10 : Lancer toute la suite**

Run :

```bash
python -m unittest discover -s tests
```

Expected : OK.

- [ ] **Step 11 : Commit**

```bash
git add cdp_scraper.py cdp_viewer.py tests/test_cdp_scraper.py tests/test_cdp_viewer.py
git commit -m "$(cat <<'EOF'
feat: versioning unifie 1.1.0 + option --version pour le viewer

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5 : P4 — `CHANGELOG.md`

**Files:**
- Create: `CHANGELOG.md`

- [ ] **Step 1 : Créer le CHANGELOG au format Keep a Changelog**

Créer `CHANGELOG.md` avec ce contenu :

```markdown
# Journal des modifications

Toutes les modifications notables de ce projet sont consignées ici.

Le format s'inspire de [Keep a Changelog](https://keepachangelog.com/fr/1.0.0/),
et le projet suit le [versionnage sémantique](https://semver.org/lang/fr/).
La version est unique pour l'ensemble du projet : `cdp_scraper.py` et
`cdp_viewer.py` portent le même numéro et sont publiés ensemble.

## [1.1.0] - 2026-06-21

### Ajouté
- Intégration continue GitHub Actions : la suite de tests s'exécute à chaque
  push et pull request (Ubuntu sur Python 3.10 à 3.13, Windows sur 3.12).
- Option `--version` pour `cdp_viewer.py`.
- Tests de robustesse du parsing (pages HTML figées dans `tests/fixtures/`).

### Modifié
- Python 3.10 ou supérieur est désormais requis (le viewer utilise des
  annotations évaluées à la définition, incompatibles avec 3.8/3.9, tous deux
  en fin de vie).

### Supprimé
- Options `--liste` et `--cookie-sortie` de `cdp_scraper.py`, ainsi que la
  fonction `sauvegarder_cookies` : elles servaient un script `telechargeur_batch.py`
  qui ne fait pas partie du projet.

### Versioning
- Mise en place d'une version unique de projet (SemVer). `cdp_scraper.py` et
  `cdp_viewer.py` partagent désormais le même `__version__`, bumpés ensemble.

## [1.0.0]

Première version publiée.

### Ajouté
- `cdp_scraper.py` : connexion à cahier-de-prepa.fr, exploration complète de
  l'arborescence des documents et téléchargement organisé par répertoire, y
  compris les programmes de colles. Conditions d'usage affichées et acceptées
  au premier lancement.
- `cdp_viewer.py` : visualiseur web local (bibliothèque standard, lié à
  127.0.0.1) de l'arborescence téléchargée, avec coloration syntaxique Python.
```

- [ ] **Step 2 : Vérifier le contenu**

Run :

```bash
grep -nE "\[1\.1\.0\]|\[1\.0\.0\]" CHANGELOG.md
```

Expected : les deux entrées de version sont présentes.

- [ ] **Step 3 : Commit**

```bash
git add CHANGELOG.md
git commit -m "$(cat <<'EOF'
docs: ajoute CHANGELOG.md (1.1.0 et 1.0.0)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6 : Q1 — Infrastructure de fixtures + tests `analyser_page`

**Files:**
- Create: `tests/fixtures/__init__.py` *(marqueur, vide — facultatif mais garde le dossier propre)*
- Modify: `tests/test_cdp_scraper.py`

> Les pages courtes restent en chaînes inline (comme les tests existants). Le dossier `tests/fixtures/` est créé ici pour les pages longues utilisées en Task 7.

- [ ] **Step 1 : Créer le dossier de fixtures**

Créer le fichier `tests/fixtures/__init__.py` (vide) pour matérialiser le dossier dans git.

- [ ] **Step 2 : Écrire les tests `analyser_page` (échouent si le comportement régresse)**

Dans `tests/test_cdp_scraper.py`, ajouter ces méthodes à la classe existante `TestAnalyserPage` (après `test_sous_dossier`, avant la fin de la classe) :

```python
    def test_documents_recents_ignore(self):
        # Le bloc « Documents récents » liste des docs dont le nom contient le
        # chemin (« Matière/… ») : ils doivent être ignorés (récupérés ailleurs).
        page = (
            '<section>'
            '<p class="doc"><span class="docdonnees">(pdf, 1 jan, 100 ko)</span> '
            '<a href="download?id=99&amp;v=zzz">'
            '<span class="icone"></span><span class="nom">Physique/TD/TD1</span></a></p>'
            '</section>'
        )
        _, docs = cdp_scraper.analyser_page(page, self.URL)
        self.assertEqual(docs, [])

    def test_melange_dossiers_et_documents(self):
        page = (
            '<section>'
            '<p class="rep"><a href="?rep=5">'
            '<span class="nom">Cours</span></a></p>'
            '<p class="doc"><span class="docdonnees">(pdf, 1 jan, 100 ko)</span> '
            '<a href="download?id=7&amp;v=aaa">'
            '<span class="icone"></span><span class="nom">Intro</span></a></p>'
            '</section>'
        )
        reps, docs = cdp_scraper.analyser_page(page, self.URL)
        self.assertEqual(len(reps), 1)
        self.assertEqual(reps[0]["nom"], "Cours")
        self.assertEqual(len(docs), 1)
        self.assertEqual(docs[0]["nom"], "Intro")

    def test_doc_sans_docdonnees(self):
        # Sans bloc docdonnees : type vide, mais URL toujours en &dl.
        page = (
            '<section>'
            '<p class="doc">'
            '<a href="download?id=8&amp;v=bbb">'
            '<span class="icone"></span><span class="nom">Sans type</span></a></p>'
            '</section>'
        )
        _, docs = cdp_scraper.analyser_page(page, self.URL)
        self.assertEqual(len(docs), 1)
        self.assertEqual(docs[0]["type"], "")
        self.assertEqual(
            docs[0]["url"],
            "https://cahier-de-prepa.fr/maclasse/download?id=8&dl",
        )

    def test_entites_html_dans_nom(self):
        # &amp; et accents encodés doivent être déséchappés dans le nom.
        page = (
            '<section>'
            '<p class="doc"><span class="docdonnees">(pdf, 1 jan, 100 ko)</span> '
            '<a href="download?id=9&amp;v=ccc">'
            '<span class="icone"></span>'
            '<span class="nom">Alg&egrave;bre &amp; G&eacute;om&eacute;trie</span></a></p>'
            '</section>'
        )
        _, docs = cdp_scraper.analyser_page(page, self.URL)
        self.assertEqual(docs[0]["nom"], "Algèbre & Géométrie")
```

- [ ] **Step 3 : Lancer les tests `analyser_page`**

Run :

```bash
python -m unittest tests.test_cdp_scraper.TestAnalyserPage -v
```

Expected : PASS (les 3 tests d'origine + les 4 nouveaux).

- [ ] **Step 4 : Commit**

```bash
git add tests/fixtures/__init__.py tests/test_cdp_scraper.py
git commit -m "$(cat <<'EOF'
test(scraper): couvre analyser_page (recents, melange, sans type, entites HTML)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7 : Q1 — Tests `crawler_progcolles`, `_html_progcolles` et utilitaires

**Files:**
- Create: `tests/fixtures/progcolles_pdf.html`
- Create: `tests/fixtures/progcolles_texte.html`
- Modify: `tests/test_cdp_scraper.py`

- [ ] **Step 1 : Créer la fixture « programme de colles — PDF par semaine »**

Créer `tests/fixtures/progcolles_pdf.html` :

```html
<!doctype html>
<html lang="fr">
<head><title>Programme de colles - Maths</title></head>
<body>
<section>
<p id="recherchecolle">recherche</p>
<a href="download?id=101&amp;v=aaa">Semaine 1 (pdf)</a>
<a href="download?id=102&amp;v=bbb">Semaine 2 (pdf)</a>
</section>
</body>
</html>
```

- [ ] **Step 2 : Créer la fixture « programme de colles — texte »**

Créer `tests/fixtures/progcolles_texte.html` :

```html
<!doctype html>
<html lang="fr">
<head><title>Programme de colles - Physique</title></head>
<body>
<section>
<p id="recherchecolle">recherche</p>
<script>var x = 1;</script>
<div id="icones"><img src="x.png"></div>
<article><h3>Semaine 1</h3><p>Mecanique du point</p></article>
</section>
</body>
</html>
```

- [ ] **Step 3 : Écrire les tests progcolles + utilitaires**

Dans `tests/test_cdp_scraper.py`, ajouter en haut du fichier (après les imports existants) un faux objet session et un helper de lecture de fixtures :

```python
FIXTURES = Path(__file__).resolve().parent / "fixtures"


def _lire_fixture(nom: str) -> str:
    return (FIXTURES / nom).read_text(encoding="utf-8")


class _FakeResp:
    """Réponse minimale : seul `.text` est utilisé par crawler_progcolles."""

    def __init__(self, text: str):
        self.text = text


class _FakeSession:
    """Session hors-ligne : renvoie une page figée selon l'URL demandée."""

    def __init__(self, pages: dict):
        self.pages = pages

    def get(self, url, timeout=None):
        return _FakeResp(self.pages.get(url, ""))
```

Puis ajouter ces classes de test avant `if __name__ == "__main__":` :

```python
class TestProgcolles(unittest.TestCase):
    BASE = "https://cahier-de-prepa.fr/maclasse"

    def test_mode_pdf_par_semaine(self):
        menu = '<a href="progcolles?maths">Colles de maths</a>'
        session = _FakeSession({
            self.BASE + "/docs": menu,
            self.BASE + "/progcolles?maths&tout": _lire_fixture("progcolles_pdf.html"),
        })
        res = cdp_scraper.crawler_progcolles(session, self.BASE)
        self.assertEqual(len(res), 2)
        for d in res:
            self.assertEqual(d["type"], "pdf")
            self.assertEqual(d["chemin"], "Maths/Programme de colles")
            self.assertTrue(d["url"].endswith("&dl"))
        noms = {d["nom"] for d in res}
        self.assertEqual(noms, {"Semaine 1", "Semaine 2"})  # suffixe « (pdf) » retiré

    def test_mode_texte(self):
        menu = '<a href="progcolles?phys">Colles de physique</a>'
        session = _FakeSession({
            self.BASE + "/docs": menu,
            self.BASE + "/progcolles?phys&tout": _lire_fixture("progcolles_texte.html"),
        })
        res = cdp_scraper.crawler_progcolles(session, self.BASE)
        self.assertEqual(len(res), 1)
        doc = res[0]
        self.assertIn("contenu_html", doc)
        self.assertEqual(doc["chemin"], "Physique")
        self.assertIn("Mecanique du point", doc["contenu_html"])
        self.assertNotIn("<script", doc["contenu_html"])
        self.assertNotIn("recherchecolle", doc["contenu_html"])
        self.assertNotIn('id="icones"', doc["contenu_html"])

    def test_contenu_protege_ignore(self):
        menu = '<a href="progcolles?secret">Colles</a>'
        session = _FakeSession({
            self.BASE + "/docs": menu,
            self.BASE + "/progcolles?secret&tout":
                "<html><body>Ce contenu est protégé</body></html>",
        })
        res = cdp_scraper.crawler_progcolles(session, self.BASE)
        self.assertEqual(res, [])

    def test_aucun_programme(self):
        session = _FakeSession({self.BASE + "/docs": "<html><body>rien</body></html>"})
        self.assertEqual(cdp_scraper.crawler_progcolles(session, self.BASE), [])


class TestHtmlProgcolles(unittest.TestCase):
    def test_nettoyage_et_autonomie(self):
        page = _lire_fixture("progcolles_texte.html")
        out = cdp_scraper._html_progcolles(page, "Physique")
        self.assertIsNotNone(out)
        self.assertTrue(out.lstrip().startswith("<!doctype html>"))
        self.assertIn("Programme de colles - Physique", out)
        self.assertNotIn("<script>var", out)
        self.assertNotIn("recherchecolle", out)

    def test_section_vide_renvoie_none(self):
        page = "<html><body><section></section></body></html>"
        self.assertIsNone(cdp_scraper._html_progcolles(page, "Physique"))


class TestUtilitaires(unittest.TestCase):
    def test_nom_sur_caracteres_interdits(self):
        self.assertEqual(cdp_scraper.nom_sur('a/b:c*d?"e<f>g|h'), "a_b_c_d_e_f_g_h")

    def test_nom_sur_troncature_200(self):
        self.assertEqual(len(cdp_scraper.nom_sur("x" * 300)), 200)

    def test_nom_sur_fallback(self):
        self.assertEqual(cdp_scraper.nom_sur("..."), "document")

    def test_nom_fichier_content_disposition_prioritaire(self):
        resp = _FakeResp("")
        resp.headers = {"Content-Disposition": 'attachment; filename="vrai_nom.pdf"'}
        nom = cdp_scraper.nom_fichier(resp, {"nom": "affiche", "type": "pdf"})
        self.assertEqual(nom, "vrai_nom.pdf")

    def test_nom_fichier_repli_nom_plus_extension(self):
        resp = _FakeResp("")
        resp.headers = {}
        nom = cdp_scraper.nom_fichier(resp, {"nom": "Cours", "type": "pdf"})
        self.assertEqual(nom, "Cours.pdf")

    def test_cle_rep_retire_parametres_affichage(self):
        a = cdp_scraper._cle_rep("https://x/docs?rep=7&ordre=nom&v=abc")
        b = cdp_scraper._cle_rep("https://x/docs?rep=7")
        self.assertEqual(a, b)
        self.assertEqual(a, "rep=7")

    def test_cle_rep_racine(self):
        self.assertEqual(cdp_scraper._cle_rep("https://x/docs"), "")
```

> Note : `nom_fichier` lit `resp.headers` (un dict-like). `_FakeResp` n'a pas d'attribut `headers` par défaut ; les tests qui appellent `nom_fichier` l'ajoutent explicitement.

- [ ] **Step 4 : Lancer les nouveaux tests**

Run :

```bash
python -m unittest tests.test_cdp_scraper.TestProgcolles tests.test_cdp_scraper.TestHtmlProgcolles tests.test_cdp_scraper.TestUtilitaires -v
```

Expected : PASS (tous).

- [ ] **Step 5 : Lancer toute la suite**

Run :

```bash
python -m unittest discover -s tests
```

Expected : OK.

- [ ] **Step 6 : Commit**

```bash
git add tests/fixtures/progcolles_pdf.html tests/fixtures/progcolles_texte.html tests/test_cdp_scraper.py
git commit -m "$(cat <<'EOF'
test(scraper): couvre crawler_progcolles, _html_progcolles et utilitaires

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8 : P3 — CI GitHub Actions

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1 : Créer le workflow CI**

Créer `.github/workflows/ci.yml` :

```yaml
name: CI

on:
  push:
  pull_request:

jobs:
  tests:
    name: Tests (${{ matrix.os }} / Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            python-version: "3.10"
          - os: ubuntu-latest
            python-version: "3.11"
          - os: ubuntu-latest
            python-version: "3.12"
          - os: ubuntu-latest
            python-version: "3.13"
          - os: windows-latest
            python-version: "3.12"
    steps:
      - uses: actions/checkout@v4
      - name: Configurer Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Installer les dépendances
        run: pip install -r requirements.txt
      - name: Lancer les tests
        run: python -m unittest discover -s tests
```

- [ ] **Step 2 : Valider la syntaxe YAML localement**

Run :

```bash
python -c "import yaml, sys; yaml.safe_load(open('.github/workflows/ci.yml', encoding='utf-8')); print('YAML OK')"
```

Expected : `YAML OK`. (Si PyYAML n'est pas installé, ignorer cette étape — la validation réelle se fera au push.)

- [ ] **Step 3 : Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "$(cat <<'EOF'
ci: ajoute le workflow GitHub Actions (matrice OS x Python)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4 : Pousser la branche et vérifier la CI** *(à faire uniquement quand l'utilisateur le demande)*

```bash
git push -u origin feat/v1.1-fondations-qualite
```

Puis vérifier sur GitHub (onglet Actions) que tous les jobs de la matrice passent au vert.

---

## Vérification finale

- [ ] **Suite complète verte localement**

Run :

```bash
python -m unittest discover -s tests -v
```

Expected : OK, tous les tests passent (existants + nouveaux).

- [ ] **Les deux CLI annoncent 1.1.0**

Run :

```bash
python cdp_scraper.py --version
python cdp_viewer.py --version
```

Expected : `cdp-scraper 1.1.0` et `cdp-viewer 1.1.0`.

- [ ] **Aucune trace du batch dans le code et la doc publiable**

Run :

```bash
grep -rnE "telechargeur_batch|cookie-sortie|--liste" cdp_scraper.py docs/cdp_scraper_doc.md README.md
```

Expected : aucune sortie.

---

## Couverture du spec (auto-revue)

- **S6 (code)** → Task 1. **S6 (doc)** → Task 2. Critère grep → Task 1 step 8, Task 2 step 4, vérif finale.
- **Python 3.10+** → Task 3.
- **P4 versioning** → Task 4 (bump + viewer `__version__` + `--version`). Garde-fou d'égalité → Task 4 steps 1-2-6.
- **P4 CHANGELOG** → Task 5.
- **Q1 parsing** → Task 6 (`analyser_page`) + Task 7 (`crawler_progcolles`, `_html_progcolles`, utilitaires, fixtures).
- **P3 CI** → Task 8.

Tous les points d'attention du spec sont couverts : la CI installe `requests` avant les tests (Task 8 step 1) ; les tests progcolles utilisent un faux objet `session` sans réseau (Task 7) ; aucun test ne dépend d'un port codé en dur côté progcolles.

# v1.2 « Synchronisation intelligente » — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Donner au scraper une synchro incrémentale (ne re-télécharger que le nouveau ou le modifié), une reprise des échecs, et au viewer des dates + une vue « récemment ajoutés », le tout adossé à un manifeste JSON par classe.

**Architecture:** Un nouveau module partagé `cdp_manifeste.py` détient le schéma du manifeste et toute la logique pure (chargement/écriture atomique, empreinte, planification, mises à jour). Le scraper l'utilise pour décider quoi télécharger et tenir le manifeste à jour ; le viewer le lit pour dater les fichiers. La détection de changement repose sur la chaîne `docdonnees` (type, date, taille) déjà récupérée pendant le crawl — aucune requête HTTP supplémentaire.

**Tech Stack:** Python 3.10+ (bibliothèque standard + `requests`), `unittest` (tests hors-ligne), front-end vanilla JS dans `cdp_viewer.py`.

**Spec de référence:** `docs/superpowers/specs/2026-06-22-v1.2-synchronisation-intelligente-design.md`

---

## File Structure

- **`cdp_manifeste.py`** (créé) — schéma + logique pure du manifeste. Responsabilités :
  `charger`, `enregistrer` (atomique), `empreinte`, `planifier`, `maj_entree`,
  `marquer_disparus`, `entrees_a_reprendre`. Aucune dépendance réseau. Importé par
  le scraper et le viewer.
- **`cdp_scraper.py`** (modifié) — `__version__`, `analyser_page` (conserve l'empreinte),
  `telecharger` (écriture atomique, retour enrichi), `parse_args` (`--complet`,
  `--reprise`), `main` (orchestration synchro + résumé enrichi), nouvelle fonction
  `executer_reprise`.
- **`cdp_viewer.py`** (modifié) — `__version__`, `lister_classes`/`_noeud` (exclusion des
  dotfiles), `construire_arbre` (date depuis le manifeste, repli `mtime`), front-end
  (affichage date + vue « récemment ajoutés »).
- **`tests/test_cdp_manifeste.py`** (créé) — tests du module manifeste.
- **`tests/test_cdp_scraper.py`** (modifié) — empreinte d'`analyser_page`, écriture
  atomique de `telecharger`, exclusivité des drapeaux.
- **`tests/test_cdp_viewer.py`** (modifié) — exclusion dotfiles, date sur les nœuds.
- **`CHANGELOG.md`**, **`README.md`** (modifiés) — documentation v1.2.0.

**Convention de commit :** chaque commit se termine par une ligne vide puis
`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

**Lancement de la suite de tests :** `python -m unittest discover -s tests -v`
(depuis la racine du projet `c:\Users\basti\Documents\cdp-scraper`).

---

## Task 1: Bump de version 1.1.0 → 1.2.0

**Files:**
- Modify: `cdp_scraper.py:36`
- Modify: `cdp_viewer.py:25`
- Modify: `tests/test_cdp_scraper.py:45-46`

- [ ] **Step 1: Mettre à jour le test de version attendue**

Dans `tests/test_cdp_scraper.py`, remplacer le corps de `test_version_attendue` :

```python
    def test_version_attendue(self):
        self.assertEqual(cdp_scraper.__version__, "1.2.0")
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `python -m unittest tests.test_cdp_scraper.TestVersion -v`
Expected: FAIL — `test_version_attendue` attend `"1.2.0"`, le code dit `"1.1.0"` ; `test_versions_egales` passe.

- [ ] **Step 3: Bumper les deux versions**

Dans `cdp_scraper.py` ligne 36 : `__version__ = "1.1.0"` → `__version__ = "1.2.0"`
Dans `cdp_viewer.py` ligne 25 : `__version__ = "1.1.0"` → `__version__ = "1.2.0"`

- [ ] **Step 4: Lancer les tests de version, vérifier qu'ils passent**

Run: `python -m unittest tests.test_cdp_scraper.TestVersion -v`
Expected: PASS (les deux tests).

- [ ] **Step 5: Commit**

```bash
git add cdp_scraper.py cdp_viewer.py tests/test_cdp_scraper.py
git commit -m "chore: passe scraper et viewer en 1.2.0"
```

---

## Task 2: `cdp_manifeste.py` — chargement / écriture atomique

**Files:**
- Create: `cdp_manifeste.py`
- Test: `tests/test_cdp_manifeste.py`

Le manifeste vit dans `<dossier_classe>/.cdp-manifest.json`. `charger` renvoie un
manifeste vide si absent, avertit et repart à vide si JSON corrompu, et lève
`ManifesteVersionFuture` si la version dépasse celle connue. `enregistrer` écrit de
façon atomique (fichier temporaire `.tmp` + `os.replace`).

- [ ] **Step 1: Écrire les tests**

Créer `tests/test_cdp_manifeste.py` :

```python
import unittest
import sys
import json
import tempfile
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import cdp_manifeste


class TestChargerEnregistrer(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.classe = Path(self.tmp.name)

    def tearDown(self):
        self.tmp.cleanup()

    def test_absent_renvoie_manifeste_vide(self):
        m = cdp_manifeste.charger(self.classe)
        self.assertEqual(m["version"], cdp_manifeste.VERSION)
        self.assertEqual(m["documents"], {})

    def test_round_trip(self):
        m = cdp_manifeste.charger(self.classe)
        m["documents"]["42"] = {"nom": "x.pdf"}
        cdp_manifeste.enregistrer(self.classe, m)
        relu = cdp_manifeste.charger(self.classe)
        self.assertEqual(relu["documents"]["42"]["nom"], "x.pdf")

    def test_json_corrompu_repart_a_vide(self):
        (self.classe / cdp_manifeste.NOM_FICHIER).write_text("{pas du json", encoding="utf-8")
        m = cdp_manifeste.charger(self.classe)
        self.assertEqual(m["documents"], {})

    def test_version_future_leve(self):
        futur = {"version": cdp_manifeste.VERSION + 1, "documents": {}}
        (self.classe / cdp_manifeste.NOM_FICHIER).write_text(
            json.dumps(futur), encoding="utf-8")
        with self.assertRaises(cdp_manifeste.ManifesteVersionFuture):
            cdp_manifeste.charger(self.classe)

    def test_enregistrer_est_atomique_pas_de_tmp_residuel(self):
        m = cdp_manifeste.charger(self.classe)
        cdp_manifeste.enregistrer(self.classe, m)
        residus = list(self.classe.glob("*.tmp"))
        self.assertEqual(residus, [])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `python -m unittest tests.test_cdp_manifeste -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'cdp_manifeste'`.

- [ ] **Step 3: Créer le module avec charger/enregistrer**

Créer `cdp_manifeste.py` :

```python
#!/usr/bin/env python3
"""cdp_manifeste — schéma et logique du manifeste de synchronisation.

Module partagé par cdp_scraper (écrit) et cdp_viewer (lit). Aucune dépendance
réseau : tout est testable hors-ligne. Le manifeste recense, par classe, les
documents téléchargés et leur empreinte, pour ne re-télécharger que ce qui a
changé (synchro incrémentale) et dater les fichiers dans le viewer.
"""
import json
import hashlib
import os
from pathlib import Path

VERSION = 1
NOM_FICHIER = ".cdp-manifest.json"


class ManifesteVersionFuture(Exception):
    """Le manifeste a été écrit par une version plus récente de l'outil."""


def _vide() -> dict:
    return {"version": VERSION, "classe": "", "url": "",
            "derniere_synchro": "", "documents": {}}


def charger(dossier_classe: Path) -> dict:
    """Lit le manifeste de `dossier_classe`. Renvoie un manifeste vide s'il est
    absent ou illisible (JSON corrompu → on repart à neuf, jamais de perte de
    fichiers). Lève ManifesteVersionFuture si la version dépasse VERSION."""
    chemin = Path(dossier_classe) / NOM_FICHIER
    if not chemin.is_file():
        return _vide()
    try:
        donnees = json.loads(chemin.read_text(encoding="utf-8"))
    except (ValueError, OSError):
        print(f"  [!] Manifeste illisible ({chemin}) : resynchronisation complète.")
        return _vide()
    if not isinstance(donnees, dict):
        return _vide()
    if donnees.get("version", 0) > VERSION:
        raise ManifesteVersionFuture(
            f"Manifeste en version {donnees.get('version')} > {VERSION} ; "
            "mettez l'outil à jour.")
    donnees.setdefault("documents", {})
    return donnees


def enregistrer(dossier_classe: Path, manifeste: dict) -> None:
    """Écrit le manifeste de façon atomique (fichier .tmp + renommage)."""
    dossier = Path(dossier_classe)
    dossier.mkdir(parents=True, exist_ok=True)
    cible = dossier / NOM_FICHIER
    tmp = dossier / (NOM_FICHIER + ".tmp")
    tmp.write_text(json.dumps(manifeste, ensure_ascii=False, indent=2),
                   encoding="utf-8")
    os.replace(tmp, cible)
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `python -m unittest tests.test_cdp_manifeste -v`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add cdp_manifeste.py tests/test_cdp_manifeste.py
git commit -m "feat(manifeste): module partage + charger/enregistrer atomique"
```

---

## Task 3: `empreinte()` — signal de changement

**Files:**
- Modify: `cdp_manifeste.py`
- Test: `tests/test_cdp_manifeste.py`

L'empreinte sert à détecter qu'un document a changé. Pour un document normal,
c'est la chaîne `docdonnees` brute (champ `empreinte` posé par `analyser_page`,
Task 6). Pour un contenu généré localement (programme de colles texte, champ
`contenu_html`), c'est un hash court du HTML.

- [ ] **Step 1: Écrire les tests**

Ajouter à `tests/test_cdp_manifeste.py` :

```python
class TestEmpreinte(unittest.TestCase):
    def test_doc_normal_utilise_docdonnees(self):
        doc = {"id": "1", "empreinte": "pdf, 1 jan, 100 ko"}
        self.assertEqual(cdp_manifeste.empreinte(doc), "pdf, 1 jan, 100 ko")

    def test_doc_sans_empreinte_renvoie_vide(self):
        self.assertEqual(cdp_manifeste.empreinte({"id": "1"}), "")

    def test_contenu_genere_hashe(self):
        a = cdp_manifeste.empreinte({"id": "pc_x", "contenu_html": "<p>A</p>"})
        b = cdp_manifeste.empreinte({"id": "pc_x", "contenu_html": "<p>B</p>"})
        self.assertTrue(a.startswith("h:"))
        self.assertNotEqual(a, b)

    def test_contenu_genere_stable(self):
        c1 = cdp_manifeste.empreinte({"contenu_html": "<p>même</p>"})
        c2 = cdp_manifeste.empreinte({"contenu_html": "<p>même</p>"})
        self.assertEqual(c1, c2)
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `python -m unittest tests.test_cdp_manifeste.TestEmpreinte -v`
Expected: FAIL — `AttributeError: module 'cdp_manifeste' has no attribute 'empreinte'`.

- [ ] **Step 3: Implémenter `empreinte`**

Ajouter à `cdp_manifeste.py` :

```python
def empreinte(doc: dict) -> str:
    """Chaîne d'empreinte d'un document pour détecter un changement.

    - contenu généré localement (contenu_html) → hash court "h:<sha>".
    - document normal → chaîne docdonnees brute (champ "empreinte"), ou "".
    """
    contenu = doc.get("contenu_html")
    if contenu is not None:
        return "h:" + hashlib.sha256(contenu.encode("utf-8")).hexdigest()[:16]
    return doc.get("empreinte", "") or ""
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `python -m unittest tests.test_cdp_manifeste.TestEmpreinte -v`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add cdp_manifeste.py tests/test_cdp_manifeste.py
git commit -m "feat(manifeste): empreinte (docdonnees ou hash du contenu genere)"
```

---

## Task 4: `planifier()` — catégorisation de la synchro

**Files:**
- Modify: `cdp_manifeste.py`
- Test: `tests/test_cdp_manifeste.py`

Fonction pure qui range chaque document du crawl par comparaison sur l'id avec le
manifeste. Renvoie un dict avec les listes `nouveau`, `modifie`, `a_jour`,
`a_reprendre` (docs du crawl) et `disparus` (ids présents au manifeste mais absents
du crawl). La présence sur disque est testée via `<dossier_classe>/<chemin>/<nom>`
en utilisant le `nom` réel mémorisé dans l'entrée. `complet=True` force tout en
re-téléchargement.

- [ ] **Step 1: Écrire les tests**

Ajouter à `tests/test_cdp_manifeste.py` :

```python
class TestPlanifier(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.classe = Path(self.tmp.name)

    def tearDown(self):
        self.tmp.cleanup()

    def _ecrire(self, chemin, nom):
        d = self.classe / chemin
        d.mkdir(parents=True, exist_ok=True)
        (d / nom).write_bytes(b"x")

    def _entree(self, **kw):
        base = {"url": "u", "nom": "f.pdf", "chemin": "Phys", "type": "pdf",
                "empreinte": "pdf, 1 jan, 100 ko", "taille": 1, "statut": "ok",
                "premiere_vue": "2026-01-01T00:00:00",
                "derniere_maj": "2026-01-01T00:00:00", "erreur": None}
        base.update(kw)
        return base

    def test_id_inconnu_est_nouveau(self):
        crawl = [{"id": "9", "empreinte": "pdf, 1 jan, 100 ko",
                  "chemin": "Phys", "nom": "f.pdf"}]
        plan = cdp_manifeste.planifier(crawl, cdp_manifeste._vide(), self.classe)
        self.assertEqual([d["id"] for d in plan["nouveau"]], ["9"])

    def test_empreinte_identique_et_present_est_a_jour(self):
        self._ecrire("Phys", "f.pdf")
        man = cdp_manifeste._vide()
        man["documents"]["9"] = self._entree()
        crawl = [{"id": "9", "empreinte": "pdf, 1 jan, 100 ko"}]
        plan = cdp_manifeste.planifier(crawl, man, self.classe)
        self.assertEqual([d["id"] for d in plan["a_jour"]], ["9"])

    def test_empreinte_differente_est_modifie(self):
        self._ecrire("Phys", "f.pdf")
        man = cdp_manifeste._vide()
        man["documents"]["9"] = self._entree()
        crawl = [{"id": "9", "empreinte": "pdf, 2 fev, 200 ko"}]
        plan = cdp_manifeste.planifier(crawl, man, self.classe)
        self.assertEqual([d["id"] for d in plan["modifie"]], ["9"])

    def test_fichier_absent_est_a_reprendre(self):
        man = cdp_manifeste._vide()
        man["documents"]["9"] = self._entree()  # fichier jamais écrit
        crawl = [{"id": "9", "empreinte": "pdf, 1 jan, 100 ko"}]
        plan = cdp_manifeste.planifier(crawl, man, self.classe)
        self.assertEqual([d["id"] for d in plan["a_reprendre"]], ["9"])

    def test_statut_echec_est_a_reprendre(self):
        self._ecrire("Phys", "f.pdf")
        man = cdp_manifeste._vide()
        man["documents"]["9"] = self._entree(statut="echec")
        crawl = [{"id": "9", "empreinte": "pdf, 1 jan, 100 ko"}]
        plan = cdp_manifeste.planifier(crawl, man, self.classe)
        self.assertEqual([d["id"] for d in plan["a_reprendre"]], ["9"])

    def test_id_manquant_du_crawl_est_disparu(self):
        man = cdp_manifeste._vide()
        man["documents"]["9"] = self._entree()
        plan = cdp_manifeste.planifier([], man, self.classe)
        self.assertEqual(plan["disparus"], ["9"])

    def test_complet_force_modifie(self):
        self._ecrire("Phys", "f.pdf")
        man = cdp_manifeste._vide()
        man["documents"]["9"] = self._entree()
        crawl = [{"id": "9", "empreinte": "pdf, 1 jan, 100 ko"}]
        plan = cdp_manifeste.planifier(crawl, man, self.classe, complet=True)
        self.assertEqual([d["id"] for d in plan["modifie"]], ["9"])

    def test_empreinte_vide_et_present_reste_a_jour(self):
        self._ecrire("Phys", "f.pdf")
        man = cdp_manifeste._vide()
        man["documents"]["9"] = self._entree(empreinte="")
        crawl = [{"id": "9"}]  # pas d'empreinte non plus
        plan = cdp_manifeste.planifier(crawl, man, self.classe)
        self.assertEqual([d["id"] for d in plan["a_jour"]], ["9"])
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `python -m unittest tests.test_cdp_manifeste.TestPlanifier -v`
Expected: FAIL — `AttributeError: module 'cdp_manifeste' has no attribute 'planifier'`.

- [ ] **Step 3: Implémenter `planifier`**

Ajouter à `cdp_manifeste.py` :

```python
def _present(dossier_classe: Path, entree: dict) -> bool:
    """True si le fichier réel décrit par l'entrée existe sur le disque."""
    nom = entree.get("nom")
    if not nom:
        return False
    chemin = entree.get("chemin", "")
    cible = Path(dossier_classe) / chemin / nom if chemin else Path(dossier_classe) / nom
    return cible.is_file()


def planifier(crawl: list, manifeste: dict, dossier_classe: Path,
              complet: bool = False) -> dict:
    """Range chaque document du crawl par comparaison avec le manifeste.

    Renvoie {"nouveau", "modifie", "a_jour", "a_reprendre"} (listes de docs du
    crawl) et "disparus" (ids du manifeste absents du crawl). `complet` force le
    re-téléchargement de tout ce qui est connu.
    """
    docs = manifeste.get("documents", {})
    plan = {"nouveau": [], "modifie": [], "a_jour": [], "a_reprendre": [],
            "disparus": []}
    vus = set()
    for d in crawl:
        ident = d["id"]
        vus.add(ident)
        entree = docs.get(ident)
        if entree is None:
            plan["nouveau"].append(d)
            continue
        if complet:
            plan["modifie"].append(d)
            continue
        if entree.get("statut") != "ok" or not _present(dossier_classe, entree):
            plan["a_reprendre"].append(d)
            continue
        emp_now = empreinte(d)
        emp_old = entree.get("empreinte", "")
        if emp_now and emp_old and emp_now != emp_old:
            plan["modifie"].append(d)
        else:
            plan["a_jour"].append(d)
    plan["disparus"] = [i for i in docs if i not in vus]
    return plan
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `python -m unittest tests.test_cdp_manifeste.TestPlanifier -v`
Expected: PASS (8 tests).

- [ ] **Step 5: Commit**

```bash
git add cdp_manifeste.py tests/test_cdp_manifeste.py
git commit -m "feat(manifeste): planifier (nouveau/modifie/a_jour/a_reprendre/disparus)"
```

---

## Task 5: Mises à jour du manifeste — `maj_entree`, `marquer_disparus`, `entrees_a_reprendre`

**Files:**
- Modify: `cdp_manifeste.py`
- Test: `tests/test_cdp_manifeste.py`

Trois helpers purs utilisés par le scraper : enregistrer le résultat d'un
téléchargement (en posant `premiere_vue` au premier ajout), marquer les disparus,
et lister les entrées à reprendre pour le mode `--reprise`.

- [ ] **Step 1: Écrire les tests**

Ajouter à `tests/test_cdp_manifeste.py` :

```python
class TestMajEntree(unittest.TestCase):
    def setUp(self):
        self.man = cdp_manifeste._vide()
        self.doc = {"id": "9", "url": "https://x/download?id=9&dl",
                    "nom": "listing.pdf", "chemin": "Phys", "type": "pdf",
                    "empreinte": "pdf, 1 jan, 100 ko"}

    def test_creation_pose_premiere_vue(self):
        cdp_manifeste.maj_entree(self.man, self.doc, "ok", "reel.pdf", 1234,
                                 "2026-06-22T10:00:00")
        e = self.man["documents"]["9"]
        self.assertEqual(e["premiere_vue"], "2026-06-22T10:00:00")
        self.assertEqual(e["derniere_maj"], "2026-06-22T10:00:00")
        self.assertEqual(e["nom"], "reel.pdf")
        self.assertEqual(e["taille"], 1234)
        self.assertEqual(e["statut"], "ok")
        self.assertEqual(e["empreinte"], "pdf, 1 jan, 100 ko")
        self.assertIsNone(e["erreur"])

    def test_maj_conserve_premiere_vue(self):
        cdp_manifeste.maj_entree(self.man, self.doc, "ok", "reel.pdf", 1,
                                 "2026-01-01T00:00:00")
        cdp_manifeste.maj_entree(self.man, self.doc, "ok", "reel.pdf", 2,
                                 "2026-06-22T10:00:00")
        e = self.man["documents"]["9"]
        self.assertEqual(e["premiere_vue"], "2026-01-01T00:00:00")
        self.assertEqual(e["derniere_maj"], "2026-06-22T10:00:00")

    def test_echec_enregistre_erreur(self):
        cdp_manifeste.maj_entree(self.man, self.doc, "echec", "listing.pdf", 0,
                                 "2026-06-22T10:00:00", erreur="500")
        e = self.man["documents"]["9"]
        self.assertEqual(e["statut"], "echec")
        self.assertEqual(e["erreur"], "500")


class TestMarquerDisparus(unittest.TestCase):
    def test_marque_statut_disparu(self):
        man = cdp_manifeste._vide()
        man["documents"]["9"] = {"statut": "ok"}
        cdp_manifeste.marquer_disparus(man, ["9"])
        self.assertEqual(man["documents"]["9"]["statut"], "disparu")


class TestEntreesAReprendre(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.classe = Path(self.tmp.name)

    def tearDown(self):
        self.tmp.cleanup()

    def test_selectionne_echecs_avec_url(self):
        man = cdp_manifeste._vide()
        man["documents"]["1"] = {"url": "u1", "nom": "a.pdf", "chemin": "P",
                                 "type": "pdf", "statut": "echec"}
        man["documents"]["2"] = {"url": "u2", "nom": "b.pdf", "chemin": "P",
                                 "type": "pdf", "statut": "ok"}
        (self.classe / "P").mkdir()
        (self.classe / "P" / "b.pdf").write_bytes(b"x")  # 2 présent et ok
        docs = cdp_manifeste.entrees_a_reprendre(man, self.classe)
        self.assertEqual([d["id"] for d in docs], ["1"])
        self.assertEqual(docs[0]["url"], "u1")

    def test_inclut_ok_mais_fichier_manquant(self):
        man = cdp_manifeste._vide()
        man["documents"]["3"] = {"url": "u3", "nom": "c.pdf", "chemin": "P",
                                 "type": "pdf", "statut": "ok"}  # fichier absent
        docs = cdp_manifeste.entrees_a_reprendre(man, self.classe)
        self.assertEqual([d["id"] for d in docs], ["3"])

    def test_ignore_entrees_sans_url(self):
        man = cdp_manifeste._vide()
        man["documents"]["pc_x"] = {"nom": "Programme.html", "chemin": "Maths",
                                    "statut": "echec"}  # pas d'url
        docs = cdp_manifeste.entrees_a_reprendre(man, self.classe)
        self.assertEqual(docs, [])
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `python -m unittest tests.test_cdp_manifeste.TestMajEntree tests.test_cdp_manifeste.TestMarquerDisparus tests.test_cdp_manifeste.TestEntreesAReprendre -v`
Expected: FAIL — attributs `maj_entree`/`marquer_disparus`/`entrees_a_reprendre` inexistants.

- [ ] **Step 3: Implémenter les trois helpers**

Ajouter à `cdp_manifeste.py` :

```python
def maj_entree(manifeste: dict, doc: dict, statut: str, nom: str,
               taille: int, quand: str, erreur=None) -> None:
    """Crée ou met à jour l'entrée du document `doc`. Pose `premiere_vue` au
    premier ajout et la conserve ensuite ; `derniere_maj` à chaque appel."""
    docs = manifeste.setdefault("documents", {})
    entree = docs.get(doc["id"])
    if entree is None:
        entree = {"premiere_vue": quand}
        docs[doc["id"]] = entree
    entree.update({
        "url": doc.get("url"),
        "nom": nom,
        "chemin": doc.get("chemin", ""),
        "type": doc.get("type", ""),
        "empreinte": empreinte(doc),
        "taille": taille,
        "statut": statut,
        "derniere_maj": quand,
        "erreur": erreur,
    })


def marquer_disparus(manifeste: dict, ids_disparus: list) -> None:
    """Marque `statut=disparu` les entrées listées (fichiers conservés)."""
    docs = manifeste.get("documents", {})
    for ident in ids_disparus:
        if ident in docs:
            docs[ident]["statut"] = "disparu"


def entrees_a_reprendre(manifeste: dict, dossier_classe: Path) -> list:
    """Documents à reprendre (mode --reprise) : entrées en échec OU dont le
    fichier manque, et qui ont une url (vrais téléchargements). Renvoie des docs
    prêts pour telecharger (id, url, nom, chemin, type)."""
    docs = []
    for ident, e in manifeste.get("documents", {}).items():
        if not e.get("url"):
            continue
        if e.get("statut") == "echec" or not _present(dossier_classe, e):
            docs.append({"id": ident, "url": e["url"], "nom": e.get("nom", ""),
                         "chemin": e.get("chemin", ""), "type": e.get("type", "")})
    return docs
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `python -m unittest tests.test_cdp_manifeste -v`
Expected: PASS (toute la classe de tests du manifeste).

- [ ] **Step 5: Commit**

```bash
git add cdp_manifeste.py tests/test_cdp_manifeste.py
git commit -m "feat(manifeste): maj_entree, marquer_disparus, entrees_a_reprendre"
```

---

## Task 6: `analyser_page` conserve l'empreinte `docdonnees`

**Files:**
- Modify: `cdp_scraper.py:236-252`
- Modify: `tests/test_cdp_scraper.py` (ajout d'un test ; docstring de `analyser_page`)
- Test: `tests/test_cdp_scraper.py`

Aujourd'hui `analyser_page` extrait uniquement le type depuis `docdonnees`
(`don_m.group(1).split(",")[0]`). On conserve en plus la chaîne brute complète dans
`doc["empreinte"]` (vide si pas de `docdonnees`). Le type reste inchangé.

- [ ] **Step 1: Écrire le test**

Ajouter dans `tests/test_cdp_scraper.py`, classe `TestAnalyserPage` :

```python
    def test_empreinte_conserve_docdonnees(self):
        page = (
            '<section>'
            '<p class="doc"><span class="docdonnees">(pdf, 1 jan, 100 ko)</span> '
            '<a href="download?id=42&amp;v=abcde">'
            '<span class="icone"></span><span class="nom">Cours 1</span></a></p>'
            '</section>'
        )
        _, docs = cdp_scraper.analyser_page(page, self.URL)
        self.assertEqual(docs[0]["empreinte"], "pdf, 1 jan, 100 ko")

    def test_empreinte_vide_si_pas_de_docdonnees(self):
        page = (
            '<section>'
            '<p class="doc">'
            '<a href="download?id=7&amp;v=x">'
            '<span class="nom">Sans donnees</span></a></p>'
            '</section>'
        )
        _, docs = cdp_scraper.analyser_page(page, self.URL)
        self.assertEqual(docs[0]["empreinte"], "")
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `python -m unittest tests.test_cdp_scraper.TestAnalyserPage -v`
Expected: FAIL — `KeyError: 'empreinte'`.

- [ ] **Step 3: Conserver l'empreinte dans `analyser_page`**

Dans `cdp_scraper.py`, remplacer le bloc de construction du document
(`documents.append({...})`, lignes ~236-252) par :

```python
            don_m = RE_DONNEES.search(bloc)
            type_ = ""
            empreinte = ""
            if don_m:
                empreinte = don_m.group(1).strip()
                type_ = empreinte.split(",")[0].strip().lower()
            id_doc = id_m.group(1)
            # Pour audio/vidéo/py/sql, le bloc <p class="doc"> contient D'ABORD un
            # lien « icon-play » en download?id=N&voir (page lecteur HTML, ou
            # redirection Basthon) AVANT le vrai lien de téléchargement. On ne se
            # fie donc pas au premier href : on reconstruit une URL propre depuis
            # l'id et on force &dl (download.php renvoie alors le binaire brut,
            # quel que soit le type, sans page lecteur ni redirection).
            documents.append({
                "url":  urljoin(url_page, f"download?id={id_doc}&dl"),
                "id":   id_doc,
                "nom":  nom or f"document_{id_doc}",
                "type": type_,
                "empreinte": empreinte,
            })
```

Mettre à jour la docstring de `analyser_page` (ligne ~208) pour refléter le nouveau
champ :

```python
      documents     : [{"url": abs, "id": str, "nom": str, "type": str, "empreinte": str}]
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `python -m unittest tests.test_cdp_scraper.TestAnalyserPage -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add cdp_scraper.py tests/test_cdp_scraper.py
git commit -m "feat(scraper): analyser_page conserve l'empreinte docdonnees"
```

---

## Task 7: `telecharger` — écriture atomique + retour enrichi

**Files:**
- Modify: `cdp_scraper.py:412-464`
- Test: `tests/test_cdp_scraper.py`

Deux changements : (1) écriture dans un fichier `.part` puis renommage atomique
(`os.replace`) pour ne jamais laisser de fichier tronqué validé ; (2) la décision
« sauter si présent » disparaît (elle remonte dans `planifier`) — `telecharger`
télécharge toujours et **écrase**. Le retour passe de `(statut, taille)` à
`(statut, taille, nom)` pour que le manifeste connaisse le nom réel enregistré.
Les statuts deviennent `ok | echec | simulation` (plus de `existe`).

- [ ] **Step 1: Écrire les tests**

Ajouter une nouvelle classe à `tests/test_cdp_scraper.py` (en réutilisant `_FakeResp`,
qu'on enrichit d'un itérateur de contenu). Ajouter au fichier :

```python
import os
import tempfile
from unittest import mock


class _RespTelecharge:
    """Réponse de téléchargement minimale pour telecharger (stream=True)."""

    def __init__(self, contenu: bytes, headers=None):
        self._contenu = contenu
        self.headers = headers or {}

    def raise_for_status(self):
        pass

    def iter_content(self, chunk_size=65536):
        yield self._contenu

    def close(self):
        pass


class _SessionTelecharge:
    def __init__(self, contenu: bytes, headers=None):
        self._resp = _RespTelecharge(contenu, headers)

    def get(self, url, timeout=None, stream=False):
        return self._resp


class TestTelechargerAtomique(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.base = Path(self.tmp.name)

    def tearDown(self):
        self.tmp.cleanup()

    def test_ecrit_le_fichier_et_renvoie_nom(self):
        sess = _SessionTelecharge(b"PDFDATA")
        doc = {"url": "https://x/download?id=1&dl", "id": "1",
               "nom": "cours.pdf", "type": "pdf", "chemin": "Phys"}
        statut, taille, nom = cdp_scraper.telecharger(sess, doc, self.base, False, 1, 1)
        self.assertEqual(statut, "ok")
        self.assertEqual(taille, 7)
        self.assertEqual((self.base / "Phys" / nom).read_bytes(), b"PDFDATA")

    def test_ecrase_un_fichier_existant(self):
        (self.base / "Phys").mkdir(parents=True)
        (self.base / "Phys" / "cours.pdf").write_bytes(b"VIEUX")
        sess = _SessionTelecharge(b"NOUVEAU")
        doc = {"url": "https://x/download?id=1&dl", "id": "1",
               "nom": "cours.pdf", "type": "pdf", "chemin": "Phys"}
        statut, _, nom = cdp_scraper.telecharger(sess, doc, self.base, False, 1, 1)
        self.assertEqual(statut, "ok")
        self.assertEqual((self.base / "Phys" / "cours.pdf").read_bytes(), b"NOUVEAU")

    def test_pas_de_part_residuel_apres_succes(self):
        sess = _SessionTelecharge(b"DATA")
        doc = {"url": "https://x/download?id=1&dl", "id": "1",
               "nom": "cours.pdf", "type": "pdf", "chemin": ""}
        cdp_scraper.telecharger(sess, doc, self.base, False, 1, 1)
        self.assertEqual(list(self.base.glob("*.part")), [])

    def test_contenu_genere_ecrit_html(self):
        doc = {"id": "pc_x", "nom": "Programme.html", "chemin": "Maths",
               "contenu_html": "<p>colles</p>"}
        statut, _, nom = cdp_scraper.telecharger(None, doc, self.base, False, 1, 1)
        self.assertEqual(statut, "ok")
        self.assertEqual(nom, "Programme.html")
        self.assertTrue((self.base / "Maths" / "Programme.html").is_file())
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `python -m unittest tests.test_cdp_scraper.TestTelechargerAtomique -v`
Expected: FAIL — `telecharger` renvoie un 2-uplet (ValueError au unpack en 3) et n'écrit pas en `.part`.

- [ ] **Step 3: Réécrire `telecharger`**

Dans `cdp_scraper.py`, remplacer toute la fonction `telecharger` (lignes 412-464) par :

```python
def telecharger(session, doc, dossier_base: Path, simulation: bool, i: int, total: int):
    """Télécharge `doc` sous `dossier_base`. Écriture atomique (.part puis
    renommage). Écrase toujours la cible (la décision de sauter est prise en
    amont par la planification). Renvoie (statut, taille, nom_reel) où statut ∈
    {ok, echec, simulation}."""
    chemin_rel = doc.get("chemin", "")
    dossier = dossier_base / chemin_rel if chemin_rel else dossier_base
    prefixe = f"[{i}/{total}]"

    # Contenu généré localement (programme de colles textuel) : pas de requête.
    if "contenu_html" in doc:
        donnees = doc["contenu_html"].encode("utf-8")
        nom = nom_sur(doc["nom"])
        affiche = f"{chemin_rel + '/' if chemin_rel else ''}{doc['nom']}"
        if simulation:
            print(f"  {prefixe} {cyan('[SIM]')} {affiche}  {dim('(' + fmt_taille(len(donnees)) + ')')}")
            return "simulation", len(donnees), nom
        dossier.mkdir(parents=True, exist_ok=True)
        _ecrire_atomique(dossier / nom, donnees)
        print(f"  {prefixe} {vert('[OK]')}  {affiche}  {dim('(' + fmt_taille(len(donnees)) + ')')}")
        return "ok", len(donnees), nom

    try:
        resp = session.get(doc["url"], timeout=60, stream=True)
        resp.raise_for_status()
    except requests.RequestException as e:
        print(rouge(f"  {prefixe} [ERR] {doc['nom']} -> {e}"))
        return "echec", 0, doc["nom"]

    nom = nom_fichier(resp, doc)
    affiche = f"{chemin_rel + '/' if chemin_rel else ''}{nom}"

    if simulation:
        taille = int(resp.headers.get("Content-Length", 0))
        print(f"  {prefixe} {cyan('[SIM]')} {affiche}  {dim('(' + (fmt_taille(taille) if taille else '?') + ')')}")
        resp.close()
        return "simulation", taille, nom

    dossier.mkdir(parents=True, exist_ok=True)
    cible = dossier / nom
    part = cible.with_name(cible.name + ".part")
    taille = 0
    try:
        with open(part, "wb") as f:
            for chunk in resp.iter_content(chunk_size=65536):
                if chunk:
                    f.write(chunk)
                    taille += len(chunk)
        os.replace(part, cible)
    except (requests.RequestException, OSError) as e:
        try:
            part.unlink(missing_ok=True)
        except OSError:
            pass
        print(rouge(f"  {prefixe} [ERR] {affiche} -> {e}"))
        return "echec", 0, nom

    print(f"  {prefixe} {vert('[OK]')}  {affiche}  {dim('(' + fmt_taille(taille) + ')')}")
    return "ok", taille, nom
```

Ajouter le helper d'écriture atomique juste avant `telecharger` (après `nom_fichier`,
vers la ligne 410) :

```python
def _ecrire_atomique(cible: Path, donnees: bytes):
    """Écrit `donnees` dans `cible` via un fichier .part puis renommage atomique."""
    part = cible.with_name(cible.name + ".part")
    part.write_bytes(donnees)
    os.replace(part, cible)
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `python -m unittest tests.test_cdp_scraper.TestTelechargerAtomique -v`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add cdp_scraper.py tests/test_cdp_scraper.py
git commit -m "feat(scraper): telecharger ecrit en atomique et renvoie le nom reel"
```

---

## Task 8: Drapeaux `--complet` et `--reprise` (mutuellement exclusifs)

**Files:**
- Modify: `cdp_scraper.py:562-593` (`parse_args`)
- Test: `tests/test_cdp_scraper.py`

Ajout de deux drapeaux exclusifs via un groupe `add_mutually_exclusive_group`.
argparse émet alors lui-même l'erreur d'usage si les deux sont passés ensemble.

- [ ] **Step 1: Écrire le test**

Ajouter à `tests/test_cdp_scraper.py` :

```python
class TestDrapeauxSynchro(unittest.TestCase):
    def test_complet_et_reprise_exclusifs(self):
        with mock.patch.object(sys, "argv",
                               ["cdp_scraper.py", "--complet", "--reprise"]):
            with self.assertRaises(SystemExit):
                cdp_scraper.parse_args()

    def test_complet_seul_ok(self):
        with mock.patch.object(sys, "argv", ["cdp_scraper.py", "--complet"]):
            args = cdp_scraper.parse_args()
        self.assertTrue(args.complet)
        self.assertFalse(args.reprise)

    def test_reprise_seul_ok(self):
        with mock.patch.object(sys, "argv", ["cdp_scraper.py", "--reprise"]):
            args = cdp_scraper.parse_args()
        self.assertTrue(args.reprise)
        self.assertFalse(args.complet)
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `python -m unittest tests.test_cdp_scraper.TestDrapeauxSynchro -v`
Expected: FAIL — argparse rejette `--complet`/`--reprise` inconnus (SystemExit pour les trois, mais `args.complet` n'existe pas → les deux derniers tests échouent à l'assert/attribut).

- [ ] **Step 3: Ajouter les drapeaux**

Dans `parse_args` de `cdp_scraper.py`, avant la ligne `p.add_argument("--version", ...)` :

```python
    synchro = p.add_mutually_exclusive_group()
    synchro.add_argument("--complet", action="store_true",
                         help="Ignorer le manifeste et tout re-télécharger (resynchro intégrale)")
    synchro.add_argument("--reprise", action="store_true",
                         help="Reprendre uniquement les téléchargements en échec, sans re-explorer\n"
                              "(les programmes de colles en texte ne sont pas concernés)")
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `python -m unittest tests.test_cdp_scraper.TestDrapeauxSynchro -v`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add cdp_scraper.py tests/test_cdp_scraper.py
git commit -m "feat(scraper): drapeaux --complet et --reprise (exclusifs)"
```

---

## Task 9: `main` — orchestration de la synchro incrémentale + résumé enrichi

**Files:**
- Modify: `cdp_scraper.py:596-690` (`main`)

Intègre le manifeste dans le flux normal : charger → crawler → planifier → ne
télécharger que `nouveau`/`modifie`/`a_reprendre` → mettre à jour le manifeste →
marquer les disparus → enregistrer (sauf simulation) → résumé enrichi. Cette tâche
est de l'intégration ; la logique testable est déjà couverte par les Tasks 2-7. La
vérification se fait par exécution réelle (Step 4).

- [ ] **Step 1: Ajouter l'import du module manifeste**

En tête de `cdp_scraper.py`, après `from datetime import datetime` (ligne 33) :

```python
import cdp_manifeste
```

- [ ] **Step 2: Brancher la reprise et la synchro dans `main`**

Dans `main`, juste après la connexion réussie (`print(vert("Connexion réussie."))`,
ligne ~628), insérer le court-circuit du mode reprise :

```python
    nom_classe = nom_sur(urlsplit(url).path.strip("/").split("/")[-1]) or "classe"
    dossier = Path(sortie) / nom_classe

    if args.reprise:
        executer_reprise(session, dossier, args.delai)
        return
```

Puis remplacer le bloc d'exploration + téléchargement + résumé (de
`print(f"\nExploration…")` jusqu'à la fin de `main`, lignes ~630-690) par :

```python
    # ── Exploration ──────────────────────────────────────────────────────────
    prof = "illimitée" if args.profondeur is None else args.profondeur
    print(f"\nExploration des documents (profondeur {prof}) …")
    documents = crawler(session, url, args.profondeur, args.delai)

    if not args.sans_colles:
        colles = crawler_progcolles(session, url)
        if colles:
            fusion = {d["id"]: d for d in documents}
            ajoutes = 0
            for d in colles:
                if d["id"] not in fusion:
                    fusion[d["id"]] = d
                    ajoutes += 1
            documents = list(fusion.values())
            print(dim(f"  + {ajoutes} élément(s) de programmes de colles"))

    if not documents:
        print(jaune("\nAucun document trouvé."))
        print(jaune("La classe n'a peut-être pas de documents accessibles avec ce compte."))
        sys.exit(0)

    print(f"\n{gras(str(len(documents)))} document(s) trouvé(s).")

    # ── Planification (synchro incrémentale) ──────────────────────────────────
    try:
        manifeste = cdp_manifeste.charger(dossier)
    except cdp_manifeste.ManifesteVersionFuture as e:
        print(rouge(f"\n{e}"))
        sys.exit(1)

    plan = cdp_manifeste.planifier(documents, manifeste, dossier, complet=args.complet)
    a_faire = plan["nouveau"] + plan["modifie"] + plan["a_reprendre"]
    a_faire.sort(key=lambda d: (d.get("chemin", ""), d["nom"]))

    print(f"  {gras(str(len(plan['nouveau'])))} nouveau(x), "
          f"{gras(str(len(plan['modifie'])))} mis à jour, "
          f"{gras(str(len(plan['a_reprendre'])))} à reprendre, "
          f"{dim(str(len(plan['a_jour'])) + ' à jour')}.\n")

    if not simulation:
        dossier.mkdir(parents=True, exist_ok=True)
        print(f"Destination : {gras(str(dossier.resolve()))}\n")

    total = len(a_faire)
    compteur = {"ok": 0, "echec": 0, "simulation": 0}
    volume = {"ok": 0, "simulation": 0}
    for i, doc in enumerate(a_faire, 1):
        statut, taille, nom = telecharger(session, doc, dossier, simulation, i, total)
        compteur[statut] = compteur.get(statut, 0) + 1
        if statut in volume:
            volume[statut] += taille
        if not simulation:
            erreur = "échec de téléchargement" if statut == "echec" else None
            cdp_manifeste.maj_entree(manifeste, doc, statut, nom, taille,
                                     datetime.now().isoformat(timespec="seconds"),
                                     erreur=erreur)
        if not simulation and args.delai:
            time.sleep(args.delai)

    # ── Manifeste : disparus + enregistrement (hors simulation) ───────────────
    if not simulation:
        cdp_manifeste.marquer_disparus(manifeste, plan["disparus"])
        manifeste["version"] = cdp_manifeste.VERSION
        manifeste["classe"] = nom_classe
        manifeste["url"] = url
        manifeste["derniere_synchro"] = datetime.now().isoformat(timespec="seconds")
        cdp_manifeste.enregistrer(dossier, manifeste)

    # ── Résumé ───────────────────────────────────────────────────────────────
    print()
    print(gras("─── RÉSUMÉ " + "─" * 40))
    if simulation:
        print(f"  À télécharger : {gras(str(compteur['simulation']))}")
        print(f"  Volume estimé : {gras(fmt_taille(volume['simulation']))}")
        print(f"  À jour (ignorés) : {dim(str(len(plan['a_jour'])))}")
        print(jaune("  (mode simulation — relancez sans --simulation pour télécharger)"))
    else:
        print(f"  Nouveaux / mis à jour : {vert(str(compteur['ok']))}   ({fmt_taille(volume['ok'])})")
        print(f"  À jour (ignorés)      : {dim(str(len(plan['a_jour'])))}")
        if compteur["echec"]:
            print(f"  Échecs                : {rouge(str(compteur['echec']))}   (relançables avec --reprise)")
        if plan["disparus"]:
            print(f"  Disparus du serveur   : {jaune(str(len(plan['disparus'])))}   (fichiers conservés)")
        print(f"\n  Fichiers dans : {cyan(str(dossier.resolve()))}")
    print()
```

- [ ] **Step 3: Lancer toute la suite (non-régression)**

Run: `python -m unittest discover -s tests -v`
Expected: PASS — aucune régression (les tests existants n'appellent pas `main`).

- [ ] **Step 4: Vérification manuelle (synchro réelle, deux passes)**

Run (remplacer les identifiants) :
```bash
python cdp_scraper.py --url https://cahier-de-prepa.fr/<classe> --login <id> --mdp <mdp> -s ./_essai_sync
```
Expected (1ʳᵉ passe) : tout en « nouveau », un fichier `_essai_sync/<classe>/.cdp-manifest.json` est créé.
Relancer la même commande.
Expected (2ᵉ passe) : « 0 nouveau(x), 0 mis à jour », la quasi-totalité en « à jour (ignorés) », aucun re-téléchargement.

- [ ] **Step 5: Commit**

```bash
git add cdp_scraper.py
git commit -m "feat(scraper): synchro incrementale via manifeste + resume enrichi"
```

---

## Task 10: Mode `--reprise` (reprise rapide sans crawl)

**Files:**
- Modify: `cdp_scraper.py` (nouvelle fonction `executer_reprise`, près de `main`)

Le court-circuit `if args.reprise:` a été posé en Task 9. On implémente maintenant
`executer_reprise` : lit le manifeste, sélectionne les entrées à reprendre via
`cdp_manifeste.entrees_a_reprendre`, les retélécharge sans crawler, met à jour et
ré-enregistre le manifeste.

- [ ] **Step 1: Implémenter `executer_reprise`**

Ajouter dans `cdp_scraper.py`, juste avant `def main():` :

```python
def executer_reprise(session, dossier: Path, delai: float):
    """Mode --reprise : retélécharge les seuls échecs/manquants listés au
    manifeste, sans re-explorer l'arborescence."""
    try:
        manifeste = cdp_manifeste.charger(dossier)
    except cdp_manifeste.ManifesteVersionFuture as e:
        print(rouge(f"\n{e}"))
        sys.exit(1)

    if not manifeste.get("documents"):
        print(jaune("\nAucun manifeste à reprendre."))
        print(jaune("Lancez d'abord une synchronisation normale."))
        return

    a_faire = cdp_manifeste.entrees_a_reprendre(manifeste, dossier)
    if not a_faire:
        print(vert("\nRien à reprendre : tout est à jour."))
        return

    a_faire.sort(key=lambda d: (d.get("chemin", ""), d["nom"]))
    total = len(a_faire)
    print(f"\nReprise de {gras(str(total))} téléchargement(s) en échec …\n")

    repris = 0
    for i, doc in enumerate(a_faire, 1):
        statut, taille, nom = telecharger(session, doc, dossier, False, i, total)
        erreur = "échec de téléchargement" if statut == "echec" else None
        cdp_manifeste.maj_entree(manifeste, doc, statut, nom, taille,
                                 datetime.now().isoformat(timespec="seconds"),
                                 erreur=erreur)
        if statut == "ok":
            repris += 1
        if delai:
            time.sleep(delai)

    manifeste["derniere_synchro"] = datetime.now().isoformat(timespec="seconds")
    cdp_manifeste.enregistrer(dossier, manifeste)

    persistants = total - repris
    print()
    print(gras("─── RÉSUMÉ " + "─" * 40))
    print(f"  Repris            : {vert(str(repris))}")
    if persistants:
        print(f"  Échecs persistants : {rouge(str(persistants))}")
    print()
```

- [ ] **Step 2: Lancer toute la suite (non-régression)**

Run: `python -m unittest discover -s tests -v`
Expected: PASS.

- [ ] **Step 3: Vérification manuelle du message sans manifeste**

Run: `python cdp_scraper.py --url https://cahier-de-prepa.fr/<classe> --login <id> --mdp <mdp> -s ./_essai_vide --reprise`
Expected : message « Aucun manifeste à reprendre. Lancez d'abord une synchronisation normale. », sortie propre (pas de crawl).

- [ ] **Step 4: Commit**

```bash
git add cdp_scraper.py
git commit -m "feat(scraper): mode --reprise (reprise ciblee sans re-crawl)"
```

---

## Task 11: Viewer — exclusion des fichiers/dossiers cachés (dotfiles)

**Files:**
- Modify: `cdp_viewer.py:543-547` (`lister_classes`), `cdp_viewer.py:550-565` (`_noeud`)
- Test: `tests/test_cdp_viewer.py`

Le `.cdp-manifest.json` (et tout nom commençant par `.`) ne doit pas apparaître dans
l'arbre ni être pris pour une classe.

- [ ] **Step 1: Écrire les tests**

Ajouter à `tests/test_cdp_viewer.py` :

```python
class TestExclusionDotfiles(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.racine = Path(self.tmp.name)
        (self.racine / "PCSI").mkdir()
        (self.racine / "PCSI" / "cours.pdf").write_bytes(b"x")
        (self.racine / "PCSI" / ".cdp-manifest.json").write_text("{}")
        (self.racine / ".cache").mkdir()

    def tearDown(self):
        self.tmp.cleanup()

    def test_classes_ignorent_les_dotdirs(self):
        self.assertEqual(cdp_viewer.lister_classes(self.racine), ["PCSI"])

    def test_arbre_ignore_le_manifeste(self):
        arbre = cdp_viewer.construire_arbre(self.racine, "PCSI")
        noms = [e["nom"] for e in arbre["enfants"]]
        self.assertIn("cours.pdf", noms)
        self.assertNotIn(".cdp-manifest.json", noms)
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `python -m unittest tests.test_cdp_viewer.TestExclusionDotfiles -v`
Expected: FAIL — `.cache` listé comme classe et `.cdp-manifest.json` présent dans l'arbre.

- [ ] **Step 3: Filtrer les dotfiles**

Dans `cdp_viewer.py`, `lister_classes` (ligne ~547), remplacer la dernière ligne par :

```python
    return sorted(p.name for p in racine.iterdir()
                  if p.is_dir() and not p.name.startswith("."))
```

Dans `_noeud` (ligne ~554), remplacer la construction des enfants par :

```python
        enfants = [_noeud(racine, p) for p in chemin_abs.iterdir()
                   if not p.name.startswith(".")]
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `python -m unittest tests.test_cdp_viewer.TestExclusionDotfiles -v`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add cdp_viewer.py tests/test_cdp_viewer.py
git commit -m "feat(viewer): ignore les fichiers caches (.cdp-manifest.json)"
```

---

## Task 12: Viewer — date des fichiers depuis le manifeste (repli `mtime`)

**Files:**
- Modify: `cdp_viewer.py:550-578` (`_noeud`, `construire_arbre`)
- Test: `tests/test_cdp_viewer.py`

Chaque nœud *fichier* gagne un champ `date` (ISO). Source : `premiere_vue` du
manifeste si l'entrée existe (indexée par chemin relatif `<classe>/<chemin>/<nom>`),
sinon le `mtime` du fichier. `construire_arbre` charge le manifeste de la classe et
le passe à `_noeud`.

- [ ] **Step 1: Écrire les tests**

Ajouter à `tests/test_cdp_viewer.py` (imports `cdp_manifeste`, `datetime` requis en
tête si absents) :

```python
import cdp_manifeste


class TestDatesArbre(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.racine = Path(self.tmp.name)
        (self.racine / "PCSI" / "Phys").mkdir(parents=True)
        (self.racine / "PCSI" / "Phys" / "tp.pdf").write_bytes(b"x")
        (self.racine / "PCSI" / "Phys" / "sans_manif.pdf").write_bytes(b"y")
        man = cdp_manifeste._vide()
        man["documents"]["1"] = {
            "url": "u", "nom": "tp.pdf", "chemin": "Phys", "type": "pdf",
            "empreinte": "", "taille": 1, "statut": "ok",
            "premiere_vue": "2026-06-01T08:00:00",
            "derniere_maj": "2026-06-01T08:00:00", "erreur": None}
        cdp_manifeste.enregistrer(self.racine / "PCSI", man)

    def tearDown(self):
        self.tmp.cleanup()

    def _fichier(self, arbre, nom):
        for d in arbre["enfants"]:
            if d["type"] == "dossier":
                t = self._fichier(d, nom)
                if t:
                    return t
            elif d["nom"] == nom:
                return d
        return None

    def test_date_vient_du_manifeste(self):
        arbre = cdp_viewer.construire_arbre(self.racine, "PCSI")
        self.assertEqual(self._fichier(arbre, "tp.pdf")["date"], "2026-06-01T08:00:00")

    def test_repli_mtime_si_absent_du_manifeste(self):
        arbre = cdp_viewer.construire_arbre(self.racine, "PCSI")
        date = self._fichier(arbre, "sans_manif.pdf")["date"]
        self.assertTrue(date)          # une date ISO non vide (mtime)
        self.assertIn("T", date)
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `python -m unittest tests.test_cdp_viewer.TestDatesArbre -v`
Expected: FAIL — `KeyError: 'date'`.

- [ ] **Step 3: Indexer le manifeste et dater les nœuds**

Dans `cdp_viewer.py`, ajouter en tête (après les imports stdlib, vers la ligne 23) :

```python
from datetime import datetime
import cdp_manifeste
```

Remplacer `_noeud` (lignes ~550-565) par une version qui reçoit un index de dates :

```python
def _noeud(racine: Path, chemin_abs: Path, dates: dict) -> dict:
    """Construit récursivement le nœud (dossier ou fichier) pour `chemin_abs`.
    `dates` : index {chemin_relatif_posix: date_iso} issu du manifeste."""
    rel = chemin_abs.relative_to(racine).as_posix()
    if chemin_abs.is_dir():
        enfants = [_noeud(racine, p, dates) for p in chemin_abs.iterdir()
                   if not p.name.startswith(".")]
        # Dossiers d'abord, puis tri alphabétique insensible à la casse.
        enfants.sort(key=lambda n: (n["type"] != "dossier", n["nom"].lower()))
        return {"nom": chemin_abs.name, "type": "dossier", "chemin": rel, "enfants": enfants}
    ext = chemin_abs.suffix.lstrip(".").lower()
    date = dates.get(rel)
    if not date:
        date = datetime.fromtimestamp(chemin_abs.stat().st_mtime).isoformat(timespec="seconds")
    return {
        "nom": chemin_abs.name,
        "type": "fichier",
        "chemin": rel,
        "taille": chemin_abs.stat().st_size,
        "ext": ext,
        "date": date,
    }
```

> Note : la déduplication du filtre dotfile entre Task 11 et Task 12 est volontaire —
> Task 12 réécrit `_noeud` en entier (signature changée), le filtre y est réintégré.

Remplacer `construire_arbre` (lignes ~568-578) par :

```python
def _index_dates(dossier_classe: Path, classe: str) -> dict:
    """Index {chemin_relatif_posix: premiere_vue} depuis le manifeste de la classe.
    Les chemins sont relatifs à la racine (préfixés par le nom de classe), comme
    les `chemin` des nœuds."""
    try:
        manifeste = cdp_manifeste.charger(dossier_classe)
    except cdp_manifeste.ManifesteVersionFuture:
        return {}
    index = {}
    for e in manifeste.get("documents", {}).values():
        nom = e.get("nom")
        if not nom:
            continue
        chemin = e.get("chemin", "")
        rel = f"{classe}/{chemin}/{nom}" if chemin else f"{classe}/{nom}"
        index[rel] = e.get("premiere_vue", "")
    return index


def construire_arbre(racine: Path, classe: str) -> dict | None:
    """Arbre de la classe `classe` sous `racine`, ou None si elle n'existe pas.

    Les `chemin` des nœuds sont relatifs à `racine` (ils incluent le dossier de
    classe) et utilisables tels quels dans /file/<chemin>. Chaque fichier porte
    une `date` (premiere_vue du manifeste, sinon mtime)."""
    racine = racine.resolve()
    base = resoudre_dans_racine(racine, classe)
    if base is None or not base.is_dir():
        return None
    dates = _index_dates(base, classe)
    return _noeud(racine, base, dates)
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `python -m unittest tests.test_cdp_viewer.TestDatesArbre -v`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add cdp_viewer.py tests/test_cdp_viewer.py
git commit -m "feat(viewer): date des fichiers depuis le manifeste (repli mtime)"
```

---

## Task 13: Viewer (front-end) — affichage de la date + vue « récemment ajoutés »

**Files:**
- Modify: `cdp_viewer.py` (bloc `PAGE_HTML`, lignes 27-337)

Côté navigateur : (1) afficher la date d'ajout sur chaque ligne fichier ; (2) une
entrée « Récemment ajoutés » dans les rubriques, avec un sélecteur de fenêtre
(7 / 30 / 90 jours), qui liste à plat les fichiers dont `date` tombe dans la
fenêtre, triés du plus récent au plus ancien. Filtrage 100 % côté client à partir
du champ `date` déjà servi (Task 12). Pas de test unitaire (JS) : vérification
manuelle au Step final.

- [ ] **Step 1: Ajouter les utilitaires de date JS**

Dans `PAGE_HTML`, juste après la fonction `octets(n)` (ligne ~140), ajouter :

```javascript
function dateCourte(iso) {            // "2026-06-22T..." -> "22 juin"
  if (!iso) return "";
  const d = new Date(iso);
  if (isNaN(d)) return "";
  return d.toLocaleDateString("fr-FR", {day: "numeric", month: "long"});
}
function joursDepuis(iso) {
  if (!iso) return Infinity;
  const d = new Date(iso);
  if (isNaN(d)) return Infinity;
  return (Date.now() - d.getTime()) / 86400000;
}
```

- [ ] **Step 2: Afficher la date sur chaque ligne fichier**

Dans la fonction `ligne(noeud)`, remplacer l'affectation de `meta.textContent`
(ligne ~191-192) par :

```javascript
  if (dossier) {
    meta.textContent = "(" + nbElements(noeud) + ")";
  } else {
    const dt = dateCourte(noeud.date);
    meta.textContent = "(" + (noeud.ext ? noeud.ext.toUpperCase() + " · " : "")
                     + octets(noeud.taille) + (dt ? " · " + dt : "") + ")";
  }
```

- [ ] **Step 3: Ajouter l'état de la fenêtre et la vue « récents »**

Dans `PAGE_HTML`, après la déclaration `let classesDispo = [];` (ligne ~106),
ajouter :

```javascript
let fenetreRecents = 30;   // jours
```

Après la fonction `rechercher(arbre, q)` (vers la ligne ~279), ajouter la vue :

```javascript
// ── Vue « récemment ajoutés » (fenêtre glissante de N jours) ────────────────
function rendreRecents(arbre) {
  elExplorateur.innerHTML = "";
  const barre = document.createElement("div");
  barre.className = "fil";
  barre.appendChild(document.createTextNode("Récemment ajoutés — "));
  const sel = document.createElement("select");
  [[7, "7 jours"], [30, "30 jours"], [90, "90 jours"]].forEach(([v, lib]) => {
    const o = document.createElement("option");
    o.value = v; o.textContent = lib;
    if (v === fenetreRecents) o.selected = true;
    sel.appendChild(o);
  });
  sel.onchange = () => { fenetreRecents = +sel.value; rendreRecents(arbre); };
  barre.appendChild(sel);
  elExplorateur.appendChild(barre);

  const recents = collecter(arbre, [])
    .filter(f => joursDepuis(f.date) <= fenetreRecents)
    .sort((a, b) => new Date(b.date) - new Date(a.date));
  if (!recents.length) {
    const v = document.createElement("div"); v.className = "vide";
    v.textContent = "Aucun ajout dans les " + fenetreRecents + " derniers jours.";
    elExplorateur.appendChild(v);
    return;
  }
  const liste = document.createElement("div"); liste.className = "liste";
  recents.forEach(f => liste.appendChild(ligne(f)));
  elExplorateur.appendChild(liste);
}
```

- [ ] **Step 4: Ajouter la rubrique « Récents » et la route `#…/__recents__`**

Dans `rendreRubriques(arbre, cible)`, juste après l'ajout du lien « Accueil »
(après `elRubriques.appendChild(accueil);`, ligne ~245), ajouter le lien récents :

```javascript
  const recents = document.createElement("a");
  const surRecents = cible && cible.__recents__;
  recents.className = "rubrique" + (surRecents ? " actif" : "");
  recents.href = hashDe(arbre.chemin + "/__recents__");
  recents.appendChild(elIcone("document"));
  recents.appendChild(document.createTextNode(" Récents"));
  elRubriques.appendChild(recents);
```

Dans `naviguer()`, remplacer le bloc de fin (à partir de
`const cible = trouver(arbre, hash) || arbre;`, ligne ~297) par :

```javascript
  if (hash.endsWith("/__recents__")) {
    rendreRubriques(arbre, {__recents__: true, chemin: arbre.chemin});
    rendreRecents(arbre);
    return;
  }
  const cible = trouver(arbre, hash) || arbre;
  rendreRubriques(arbre, cible);
  rendreListe(arbre, cible);
```

- [ ] **Step 5: Vérification manuelle (viewer servi)**

Prérequis : avoir lancé une synchro (Task 9) produisant un manifeste avec des dates.
Run: `python cdp_viewer.py -s ./_essai_sync` puis ouvrir l'URL affichée.
Expected :
- chaque fichier affiche sa date (ex. « PDF · 1.2 Mo · 22 juin ») ;
- la rubrique « Récents » liste les fichiers récents, le sélecteur 7/30/90 jours
  filtre la liste ;
- `.cdp-manifest.json` n'apparaît jamais dans l'arbre.

- [ ] **Step 6: Commit**

```bash
git add cdp_viewer.py
git commit -m "feat(viewer): affichage des dates + vue « recemment ajoutes »"
```

---

## Task 14: Documentation — CHANGELOG + README

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `README.md`

- [ ] **Step 1: Lire l'existant**

Lire `CHANGELOG.md` et `README.md` pour respecter leur format (titres, style des
sections, exemples de commandes).

- [ ] **Step 2: Ajouter l'entrée CHANGELOG 1.2.0**

En tête de la liste des versions de `CHANGELOG.md`, ajouter (adapter au format
existant — date du jour 2026-06-22) :

```markdown
## [1.2.0] — 2026-06-22

### Ajouté
- Synchronisation incrémentale : seuls les documents nouveaux ou modifiés sont
  re-téléchargés, via un manifeste JSON par classe (`.cdp-manifest.json`). La
  détection des changements s'appuie sur les métadonnées déjà présentes dans le
  listing (aucune requête supplémentaire).
- `--complet` : ignore le manifeste et force une resynchronisation intégrale.
- `--reprise` : retélécharge uniquement les échecs/manquants, sans re-explorer.
- Viewer : date d'ajout sur chaque fichier et vue « Récemment ajoutés »
  (fenêtre glissante 7 / 30 / 90 jours).

### Corrigé
- Les téléchargements sont écrits de façon atomique (`.part` puis renommage) :
  une interruption ne laisse plus de fichier tronqué considéré comme complet.
- Un document mis à jour côté serveur est désormais re-téléchargé (auparavant
  tout fichier déjà présent était systématiquement ignoré).
```

- [ ] **Step 3: Mettre à jour le README**

Dans `README.md`, à l'endroit où les options/usages sont décrits, ajouter une
section sur la synchronisation (adapter au style existant) :

```markdown
## Synchronisation

Relancer `cdp_scraper.py` sur une classe déjà téléchargée ne re-télécharge que les
documents **nouveaux ou modifiés** depuis la dernière fois (synchro incrémentale,
sans requête superflue). Un manifeste `.cdp-manifest.json` est tenu à jour dans le
dossier de chaque classe.

- `--complet` : tout re-télécharger (resynchronisation intégrale).
- `--reprise` : reprendre uniquement les téléchargements en échec, sans
  re-explorer l'arborescence.

Côté viewer, chaque fichier affiche sa date d'ajout et une vue « Récemment
ajoutés » regroupe les derniers documents (7 / 30 / 90 jours).
```

- [ ] **Step 4: Commit**

```bash
git add CHANGELOG.md README.md
git commit -m "docs: documente la synchro v1.2 (manifeste, --complet, --reprise)"
```

---

## Task 15: Vérification finale & non-régression

**Files:** aucun (validation)

- [ ] **Step 1: Lancer toute la suite de tests**

Run: `python -m unittest discover -s tests -v`
Expected: PASS — tous les tests (manifeste, scraper, viewer), aucun échec ni erreur.

- [ ] **Step 2: Vérifier le compte de tests et l'absence de fichiers oubliés**

Run: `git status`
Expected: arbre propre (tout est commité). Vérifier qu'aucun dossier `_essai_*` de
test manuel n'est suivi par git (ils sont sous des chemins ignorés type `cours_cdp/`
seulement si nommés ainsi ; sinon les supprimer manuellement).

- [ ] **Step 3: Vérifier `--help`**

Run: `python cdp_scraper.py --help`
Expected: `--complet` et `--reprise` apparaissent avec leur aide ; ils sont
mutuellement exclusifs (tester `--complet --reprise` → erreur d'usage argparse).

- [ ] **Step 4: Récapitulatif**

Confirmer que les quatre items de la spec sont couverts :
- **S2** manifeste JSON → `cdp_manifeste.py` (Tasks 2-5).
- **S1** synchro incrémentale → `analyser_page` empreinte + `planifier` + `main`
  (Tasks 4, 6, 9) + écriture atomique (Task 7).
- **S4** reprise des échecs → run normal (`a_reprendre`) + `--reprise` (Tasks 9-10).
- **V1** dates + récents → viewer (Tasks 11-13).

---

## Self-Review (à exécuter par l'auteur du plan, déjà fait)

- **Couverture spec :** S2 (Tasks 2-5), S1 (4, 6, 7, 9), S4 (9, 10), V1 (11, 12, 13),
  versioning (1), gestion d'erreurs manifeste corrompu/version future (2, 9, 10),
  écriture atomique (2, 7), compat ascendante (couverte par `planifier` : fichiers
  présents sans entrée → `nouveau` mais non re-téléchargés si déjà là… ⚠️ voir note),
  docs (14). Tous les points de la spec ont une tâche.
- **Note de compat ascendante :** pour un dossier v1.1 sans manifeste, tous les
  documents sont `nouveau` (pas d'entrée) → ils seront re-téléchargés une fois pour
  amorcer le manifeste. C'est conforme à la spec (« premier run v1.2 le
  reconstruit ») ; `telecharger` écrase à l'identique, sans perte. Acceptable.
- **Cohérence des types :** `telecharger` renvoie partout `(statut, taille, nom)` ;
  `maj_entree(manifeste, doc, statut, nom, taille, quand, erreur=None)` appelée de
  façon cohérente dans `main` et `executer_reprise` ; `planifier` renvoie les clés
  `nouveau/modifie/a_jour/a_reprendre/disparus` utilisées telles quelles ; `_noeud`
  prend `(racine, chemin_abs, dates)` partout après la Task 12.
- **Placeholders :** aucun TBD/TODO ; chaque étape de code montre le code réel.
